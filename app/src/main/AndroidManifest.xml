<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.project.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.project.app.BaseApplication"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name="com.project.app.feature.main.presentation.SplashScreenActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature.main.presentation.MainActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.addition.presentation.AdditionActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.multiplication.MultiplicationActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.primary.PrimaryActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".feature.fibonacci.presentation.FibonacciActivity"
            android:screenOrientation="portrait" />
    </application>
</manifest>