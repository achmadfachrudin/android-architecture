<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View"/>

        <variable
                name="view"
                type="com.project.app.presentation.main.adapter.MenuListItemView"/>

        <variable
                name="vm"
                type="com.project.app.presentation.main.adapter.MenuListItemViewModel"/>

    </data>

    <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:onClick="@{v ->view.onClickMenu(v)}"
            android:layout_height="wrap_content">

        <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="80dp"
                android:padding="16dp"
                android:orientation="horizontal">

            <ImageView
                    android:id="@+id/img_menu"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:scaleType="centerCrop"
                    tools:layout_width="30dp"
                    android:layout_gravity="center_vertical"
                    android:visibility="@{safeUnbox(vm.bShowImage) ? View.VISIBLE : View.GONE}"
                    android:contentDescription="@string/app_name"/>

            <TextView
                    style="@style/RegularFont"
                    android:paddingStart="16dp"
                    android:paddingEnd="16dp"
                    android:layout_width="match_parent"
                    android:layout_gravity="center_vertical"
                    android:layout_height="wrap_content"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:text="@{vm.bTextTitle}"
                    tools:text="@string/msg_test"/>
        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>